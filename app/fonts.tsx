import localFont from "next/font/local";

export const moredya = localFont({
  src: "./Moredya.ttf",
  display: "swap",
});

export const zebulon = localFont({
  src: "./zebulon.otf",
  display: "swap",
});
